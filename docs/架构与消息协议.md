# 架构与消息协议

## 一、责任边界

| 模块 | 责任 | 不应承担 |
| --- | --- | --- |
| `background/service-worker.js` | 观察 YouTube 字幕请求、保存标签页短期状态、转发命令、白名单代理哔哩哔API、Alarms 定时巡检与 Badge 管理；拥有 Queue 存储/执行与 Native Messaging 的唯一运行时入口；按真实视频路由懒注入完整播放器运行时 | 解析字幕 UI、操作页面 DOM、代理任意未知网址 |
| `content/main-world-bridge.js` | 从 YouTube 播放器实例读取实时轨道，请求播放器切换轨道 | 渲染界面、持久化数据、替换全局网络函数 |
| `platform/*.js` | 屏蔽 YouTube/哔哩哔接口差异，输出统一轨道与字幕数组、选集拓扑分析与 DASH 直链 | 操作双界面 |
| `core/parsers.js` / `formatters.js` | 纯解析、时间定位、格式化 (Markdown/SRT/VTT) 和通用工具 | 依赖具体页面结构 |
| `core/media.js` | 当前 `<video>` 解析、目标时间 seek、缓冲/解码就绪判定、Canvas 原画编码、瞬态失败有限重试与播放位置恢复 | 章节规划、证据语义、Side Panel 进度或第二套重试策略 |
| `core/visual-state-detector.js` | 生成少量候选时间、像素签名评分、跨窗口去重与证据预算 | 操作播放器、等待网络缓冲、直接更新 UI |
| `core/batch-export.js` | 统一批量条目选择、结果排序、复制/合并文件/ZIP 输出投影与最终投递 Interface | 抓取平台字幕、理解 B站/YouTube 原始接口 |
| `core/tracker.js` | 纯 JS MD5, WBI 签名加密, YouTube RSS 解析, 环形缓冲, 自动字幕拉取与存储 | 操作具体 UI 界面 |
| `core/queue.js` | 持久任务、执行租约、平台字幕优先级、本机 ASR 回退与结果落盘 | 直接建立 Native Messaging 连接、持久化 Bilibili 签名音频 URL |
| `core/queue-orchestrator.js` | 合并并发唤醒，保证单飞执行与尾部补跑 | 解析字幕或拥有第二套队列状态 |
| `core/native-host.js` | 复用 Native Messaging Port、请求关联、活动看门狗、分块结果校验；生产运行时仅由 Service Worker 持有 | 传输媒体字节、读取 Cookie、直接渲染 UI；被 Side Panel/content script 再加载成第二个 Native client |
| `content/app.js` | 当前视频标签页唯一状态源、路由调度、取消旧任务、播放同步、作者与合集元数据提取 | 包含大段平台特定解析代码、拥有 Queue/Tracker/Native 核心 |
| `content/feed-injector.js` | 推荐流/搜索/空间等普通站点页的轻量按钮与 Queue projection 消费；检测同文档进入真实视频路由后请求后台按需加载播放器运行时 | 直接加载 Queue/Tracker/AI 核心、直接修改 Queue 存储、执行转录 |
| `content/rolling-panel.js` | 播放器旁紧凑滚动面板（Shadow DOM 隔离） | 直接请求字幕接口 |
| `sidepanel/*` | 完整搜索、跳转、复制、AI总结、导出与订阅追踪看板；Queue 只通过 Service Worker 的 summary/projection/detail/settings 消息消费任务状态 | 拥有另一套独立字幕状态、加载 `core/queue.js`、直接建立 Native Messaging Port |
| `native/SparkSubHost` | **兼容/回滚 Native Messaging 实现**：保留 v1/v2、旧 macOS 14+ 本机执行和故障回退 | 作为默认模型策略或继续扩展新的浏览器执行能力；默认执行已由 SparkScribe bundled helper 承接 |

## 二、核心状态

`content/app.js` 是当前标签页的唯一状态源，状态主要字段为：

```text
platform          平台 (youtube / bilibili)
mediaKey          规范化视频标识
status            idle / loading / ready / empty / error
isRefreshing      已有字幕时的非破坏性后台刷新
revision          单调递增的状态版本，拒绝旧状态回写
cueRevision       仅在新字幕正文提交时递增，避免诊断更新重建大列表
tracks            字幕轨道列表
selectedTrackId   当前轨道
cues              统一字幕数组
activeIndex       当前播放句
authorInfo        作者名字、MID/ChannelID、头像、合集ID与名称
diagnosticSessionId 当前媒体诊断会话标识
diagnostics       已脱敏的 DiagnosticEvent[] 结构化阶段日志
lastError         阶段 / 错误代码 / 消息 / 处理建议
```

滚动面板和侧边栏只消费这份状态，不各自请求字幕，从而保证轨道、当前句和错误状态一致。

### 结构化诊断契约

每条 `DiagnosticEvent` 包含 `level`、`scope`、`code`、`stage`、`message`、`sessionId`、时间戳和白名单 context。当前视频使用 `media` scope；侧栏另外隔离 `queue`、`native` 和 `batch`。所有事件必须经过共享 store 的 `append()`，统一执行脱敏、短窗去重、单会话裁剪和总量裁剪，UI 不得直接维护字符串日志数组。

默认诊断视图显示 `info`、`warn` 和 `error`，详细视图才显示 `debug`。HTTP 状态、接口通道、CID、缓存和挂载调度属于 debug；预期降级属于 info 或 warn；只有工作流最终失败使用 error。侧栏复制日志时只复制当前选择的 scope 和活动会话。

## 三、消息协议

| 消息 | 方向 | 用途 |
| --- | --- | --- |
| `BSE_CAPTION_REQUEST_CAPTURED` | 后台 → 页面 | 通知页面捕获到的 YouTube 原生字幕请求 |
| `BSE_GET_CAPTURED_REQUESTS` | 页面 → 后台 | 恢复当前标签页的字幕请求地址 |
| `BSE_FETCH_BILIBILI_RESOURCE` | 页面/侧边栏 → 后台 | 仅读取白名单内的哔哩哔 API/CDN JSON 资源 |
| `BSE_DOWNLOAD_MEDIA_FILE` | 页面/侧边栏 → 后台 | 调用 Chrome Downloads 管理器代理下载媒体文件 |
| `BSE_STATE_UPDATE` | 页面 → 后台 | 发布轨道、字幕和诊断状态 |
| `BSE_PLAYBACK_UPDATE` | 页面 → 后台 | 仅发布高频变化的当前句索引 |
| `BSE_STATE_BROADCAST` | 后台 → 侧边栏 | 将完整状态同步给侧边栏 |
| `BSE_PLAYBACK_BROADCAST` | 后台 → 侧边栏 | 更新当前句，避免反复传输全部字幕 |
| `BSE_COMMAND_ACTIVE_TAB` | 侧边栏 → 后台 | 将跳转、切轨和刷新命令发给当前视频页 |
| `BSE_ENSURE_VIDEO_RUNTIME` | 轻量 Feed/bootstrap → 后台 | 普通站点页同文档进入真实视频路由时先 ping 当前标签页；仅在完整视频运行时尚未安装时按当前平台补注入 |
| `BSE_TRACKER_CHECK_NOW` | 侧边栏 → 后台 | 手动立即触发全量订阅源增量检测与字幕抓取 |
| `BSE_QUEUE_ENQUEUE` | 扩展页面 → 后台 | 写入 URL、`sourceLanguage` 与处理意图；当前视频显式 `local-asr` 还必须携带已验证的 `mediaKey`，随后唤醒唯一队列执行器 |
| `BSE_QUEUE_RETRY` / `REMOVE` / `CLEAR_ALL` | 扩展页面 → 后台 | 由唯一执行器先取消活动任务，再原子修改队列 |
| `BSE_QUEUE_GET` / `GET_ITEM` / `GET_SUMMARY` | Feed / Side Panel → 后台 | 分别读取 list projection、单个 canonical item 和轻量 summary；普通列表/角标不得因此搬运历史 cues |
| `BSE_QUEUE_GET_SETTINGS` / `SAVE_SETTINGS` | Side Panel → 后台 | 读写 Queue 用户设置；Side Panel 不加载 Queue 核心实现 |
| `BSE_ORCHESTRATOR_NOTIFY` | 扩展上下文 → 后台 | 仅发送“可能有工作”信号；调用者不直接执行队列 |
| `BSE_NATIVE_CAPABILITIES` | 侧边栏 → 后台 | 通过可信扩展页代理读取 `sparkscribe.browser-native/2` 的能力视图：本机 ASR 语言、YouTube captions/media、Bilibili media、取消与分块结果 |
| `BSE_NATIVE_CANCEL` | 侧边栏/队列 → 后台 | 通过 Service Worker 代理取消指定本机任务；YouTube 批量任务 Abort 时同时本地结束 UI Promise 并发送真实 jobId 取消 Host 工作 |
| `BSE_CAPTURE_FRAME` | 侧边栏 → 页面 | 单点原画 Canvas 截帧；显式时间点由 Media Module 等待 seek 与当前解码帧可读后再编码。异步操作可携带 `expectedMediaKey`，页面只在当前媒体身份一致时执行并在响应中回传统一的 `error/message/mediaKey` |
| `BSE_CAPTURE_BEST_FRAME` | 侧边栏 → 页面 | 阶段一唯一的“窗口 → 代表帧”媒体 Interface：在规划窗口内采样少量真实画面并选择代表帧；缓冲/解码瞬态失败由 Media Module 内部有限重试，调用方不解释播放器错误码；同样受 `expectedMediaKey` 约束并恢复原播放状态 |
| `BSE_GET_MEDIA_CONTEXT` | 侧边栏 → 页面 | AI 处理前按当前 `expectedMediaKey` 获取/刷新规范化 `MediaContext`，用于确认标题与标签；失败可继续使用已知语境，但跨媒体必须拒绝 |
| `BSE_FETCH_LOCAL_LLM` | 页面/侧边栏 → 后台 | 代理请求 `http://localhost` / `http://127.0.0.1` 本地 AI 网关，支持 Bearer Auth；不承担任意公网代理 |

## 四、AI 图文报告与多模态视频理解流水线

SparkSub 对用户只暴露两个阶段：**阶段一确定结构并取得证据，阶段二按既定结构写笔记**。阶段一内部仍拆成文本规划与播放器取帧两个技术步骤，但播放器执行不是第三个用户任务：

1. **阶段一 A：章节骨架 + 取样窗口规划 (Chapter & Sampling Planning)**：
   * 输入当前视频完整的带时间戳字幕 `cues[]`。发给模型的字幕使用 `00:12  文本` 这种普通时间列；这一阶段不写最终笔记，也不假装已经看到了真实画面；
   * 规划结果包含轻量 `summary`、`chapters[]` 和 `samplingWindows[]`。`chapters` 只保留 `id / title / windowStart / windowEnd / coreConcept`，标题可直接作为最终笔记大标题，数值时间范围是后续唯一的章节定位事实，不另维护冗余 `timeStr`；`normalizePlanChapters()` 负责吸收历史 `startSec/endSec/coreIdea` 等别名并按时间排序；
   * `samplingWindows[]` 使用 `chapterId / windowStart / windowEnd / targetSec / contentHint / samplingGoal / reason / importance`。`chapterId` 把证据绑定到章节，窗口与目标秒只服务播放器寻帧，`contentHint` 描述预期视觉类型，`samplingGoal` 描述画面应补充什么，`reason` 只解释为什么字幕不足，`importance` 服务后续证据预算；
   * 只有公式/板书、图表、题目、代码/界面、实验步骤、作品/文献/场景或明确视觉指代等字幕无法完整承载的信息才安排取样。纯口述已经足够时不为凑图创建窗口；同类近邻窗口应合并；
   * `normalizeSamplingWindows()` 是规划协议到媒体执行层的翻译 seam：新 `samplingWindows` 与历史 `visualRequests / visualEvidence` 都在这里归一，因此旧外部规划 JSON 继续兼容，而新模型交互不暴露媒体执行层内部术语；
   * 请求失败与返回格式失败必须区分：认证/网络/模型调用失败直接终止本次生成；模型已成功返回但规划 JSON 不合法时，才允许降级为“字幕 + 用户手动画面”继续合成。
2. **阶段一 B：窗口内真实筛帧 + 跨窗口证据精简 (Visual Candidate Selection)**：
   * 每个媒体执行请求通过 `BSE_CAPTURE_BEST_FRAME` 在目标附近最多取 3 个候选秒数；候选只生成约 `64×36` 的灰度像素签名，比较帧间变化、边缘细节与曝光。进入跨窗口去重前再压缩成保留二维空间结构的约 128 维网格 fingerprint，避免白底 PPT、代码页或公式页仅因整体灰度接近就被误判成同一画面；
   * 显式 seek 只有在 `currentTime` 已进入目标容差、`seeking=false`、`readyState >= HAVE_CURRENT_DATA` 且视频尺寸有效时才允许编码高清帧；收到 `seeked` 但目标帧仍未缓冲/解码时继续等待媒体事件，超时才返回稳定错误。`requestAnimationFrame` / `requestVideoFrameCallback` 属于页面呈现节奏，不作为截图成功前提，因此普通窗口失焦或后台标签页不会因为绘制节流而被误判失败；浏览器若已把标签页冻结/丢弃，则仍按不可执行状态失败，不伪造截图；
   * `TARGET_NOT_BUFFERED / FRAME_NOT_DECODED / NO_STABLE_FRAME / FINAL_FRAME_NOT_READY` 等瞬态媒体错误只在 `core/media.js` 内用于一次有限重试；Side Panel、外部 AI Adapter 和内置 AI Adapter 都只调用一次 `BSE_CAPTURE_BEST_FRAME`。相邻候选共享同一未缓冲区时不逐点重复等超时，先结束本轮，再用更宽超时重试一次；最终失败才进入诊断与跳过逻辑；
   * 最终证据帧使用约 `1536px` 最大宽度与 `WebP 0.90` 质量，优先保住代码、公式、文献和小字号幻灯片细节；候选阶段仍然只处理低分辨率签名，因此提高最终图质量不会把多候选成本同步放大；
   * 每个窗口得到的高清图先作为**本次运行时候选**存在内存。所有窗口结束后，再按近重复度、视觉质量、重要性和时间覆盖做一次全局筛选；近重复判断会检查最近时间范围内已经保留的候选，而不是只和相邻上一张比较，因此 `A → B → A` 这类重复也能识别。用户手动截图优先保留，自动截图的预算随视频时长动态增长，默认约 6～24 张；
   * `selection.fingerprint`、视觉评分和候选签名只服务当前筛选，不写入长期缓存。最终帧继续保留 `source / chapterId / expectedSurface / evidenceGoal / importance` 等轻量语义，让阶段二知道“这张图属于哪一章、是什么、应补充什么”。若规划窗口命中用户已有的手动截图，则直接复用图片并把缺失的规划语义合并进去，不重复寻道和编码；未进入最终证据集的自动候选不会送给多模态模型，也不会进入 AI Note IndexedDB；外部规划托盘允许保留更宽的候选集合，但超过阈值后先做一轮宽松初筛；
   * 像素读取受限时明确退回纯时间启发式，并以 `selection.strategy = 'time-fallback'` 标记，不把时间猜测描述成视觉检测成功。
3. **阶段二：按既定章节骨架合成图文笔记 (Structured Multimodal Synthesis)**：
   * 阶段二不再重新切章。SparkSub 先用阶段一 `chapters.windowStart/windowEnd` 确定性地把原始 `cues[]` 分到 C01/C02… 对应章节，再把 `summary + chapters + 分组字幕 + 最终证据帧` 交给模型；这样模型只需要解释和组织内容，不再重复做章节归属判断；
   * 每个主要章节必须沿用 `## [开始时间–结束时间] 标题`，例如 `## [01:15–03:30] 切线与割线的几何关系`。章节顺序与时间范围来自阶段一；标题措辞允许轻微润色，但不能重排或重新划分。三级标题只用于章节内部确实需要的细分；
   * 最终帧优先按 `chapterId` 放回所属章节，没有 chapterId 时才按截图时间落入章节范围。`expectedSurface` 告诉模型这是 diagram/slide/interface/document 等哪类材料，`evidenceGoal` 告诉模型它应补充什么；规划阶段的 `reason` 不重复塞入最终写作指令；
   * 写作继续根据内容类型自适应：数学/理工强调对象、推导、条件与边界；软件强调目标、操作、状态与失败场景；人文社科强调事实、观点、论据、背景和不同视角；访谈、评论、艺术与纪录片使用与内容匹配的叙事结构；
   * 图片引用采用普通 Markdown 形式 `![简短说明](frame://01:15)`。`frame://` 是 SparkSub 内部图片引用 seam，渲染和 ZIP 导出时再解析成真实图片；历史缓存中的 `[SCREENSHOT: 01:15 "说明"]` 继续兼容；数学公式只在内容确实需要时使用常规 LaTeX，画面看不清时不补造细节。
4. **呈现、导出与缓存 (Presentation, Export & Cache)**：
   * 侧边栏将 AI/外部导入 Markdown 当作不可信文本处理：原始 HTML 默认转义；表格单元格逐格转义后只恢复受控表格结构；图片 URL 仅接受 `http(s)`、`blob:` 与有限的 `data:image/*;base64` 类型，禁止 `javascript:` 与 SVG Data URL 注入；
   * `renderNoteToHtml()` 会识别二级标题 `## [MM:SS–MM:SS] 标题`（同样支持 HH:MM:SS），渲染为 `.note-timeline-heading`：显式时间范围按钮携带 `data-seek=<章节起点>`，点击后通过现有 `SEEK` 命令回到该节开头；只有时间控件跳播，标题文字仍可正常选择。普通 H2 不做特殊处理；
   * 报告内图片使用 `contain` 完整显示，不再用固定 `220px + cover` 裁掉板书、文档或代码内容；点击缩略图打开原图预览层，`▶ MM:SS` 按钮单独负责跳回视频，避免同一个点击既表示“放大”又表示“跳播”；
   * `transformFrameReferences()` 负责识别新旧图片引用，`resolveFrameEntry()` 负责把引用时间解析到真实截图。渲染、删除和 ZIP 导出共用这两个 seam；精确时间不存在时只在约 ±5 秒内选择真正最近的一张，避免页面显示、删除和导出对同一个 `frame://` 得出不同答案；
   * `frame://` 的时间是报告中的语义槽位，真实截图时间可能略有偏移。图片卡片和跳播展示/使用真实截图时间，同时删除仍保留原引用时间，从而既不伪造截图秒数，也能准确删除原 Markdown 引用；
   * 外部网页 AI 是学习产物 Interface 的另一条执行 Adapter，而不是单独的“提示词模式”。五种纯文本任务执行“复制任务 → 外部 AI → 导入结果”；`course_notes` 与内部生成使用同一个两阶段契约：“复制阶段一章节+取样任务 → 导入规划 → SparkSub 自动取帧/投递画面 → 复制阶段二任务 → 导入笔记”。导入阶段一后，Side Panel 必须保留 `summary / chapters / samplingWindows / visualEvidence` 为 `externalVideoPlan` 并传回 `buildCourseNotePrompt(videoIR=...)`，不能只留下图片后让第二个模型重新规划结构。规划 JSON 和最终 Markdown 使用两个明确入口导入；最终 Markdown 仍按当前 mode 写入独立缓存；
   * 外部网页 AI 不再提供“拖拽全部”这种跨 Side Panel → 网页上传框的不可靠文件传输承诺。直接投递改为**复制精选拼图**：底层布局也硬限制最多 4 张，1～2 张单列、3～4 张双列，画布宽约 `1152px`，竖屏/文档图也在受控高度内等比缩放；需要全部原始分辨率时使用“原图打包” ZIP；
   * 阶段二外部合成提示词只引用用户**最后一次实际投递**的图片集合：联系表按真正成功绘制进去的图片生成提示词，原图包则按已打包原图生成更宽的证据集。不会再出现“提示词列出 12 张、网页版实际只收到 4 张”的输入协议错位；
   * 联系表只服务剪贴板投递，不替代原图缓存。它严格限制总尺寸，避免无限纵向长图在视觉模型侧被整体缩小后让文字、公式和代码失去可读性；
   * 点击图片右上角 `▶ MM:SS` 可回跳视频对应画面；导出时 `frame://` / 旧 `[SCREENSHOT]` 引用统一转换为 `images/...` 的标准 Markdown 图片路径；
   * 学习/复习 Markdown 以稳定 `artifactKey` 为 owner 存于 `chrome.storage.local`（记录前缀 `bse_ai_note_v2_`，LRU 索引 `bse_ai_note_index_v2`），最终选中的截图二进制存入 IndexedDB `bse_ai_media_v1/frames`。`artifactKey` 表达用户眼中的逻辑视频内容：YouTube 固定为 `yt:<videoId>`，Bilibili 固定为 `bili:<BVID>:p<P>`；运行时用于媒体操作的 `mediaKey` 仍可更精确到 `bili:<BVID>:cid<CID>`。完整 URL 不作为缓存主键，只随产物保存为 `sourceUrl` provenance，避免时间点、播放列表、追踪参数或 CID 探测阶段变化制造重复缓存。旧版若曾按 CID key 保存同一分P，恢复时通过当前轨道的 BVID/CID/page 证明后一次性迁移到规范 `artifactKey`；保存时先写一套带版本 ID 的新图片集，不覆盖旧图，随后在一次 `chrome.storage.local.set()` 中提交 Markdown 指针与 LRU 索引，提交失败就删除本次新图片，提交成功后才清理旧图片。同一 `artifactKey` 的保存串行执行，避免补图/删除交错覆盖；
   * 缓存继续受数量、总图片字节与 30 天有效期回收策略约束；只保存最终图片及少量来源/用途元数据，质量 fingerprint、评分与临时候选不持久化。

### 媒体身份与异步操作不变量

- **媒体身份是 `tabId + mediaKey`，不是“当前活动页面”这个瞬时概念**。自动生成、外部规划截帧、手动截帧与占位补图在开始时锁定这一身份；后续请求发送到启动时的标签页，而不是每一步重新查询当前活动 tab。
- 页面侧收到 `expectedMediaKey` 后再次核对自身状态，响应也带回实际运行时 `mediaKey`；Side Panel 在发送前、响应后和最终提交前都会检查上下文。一旦用户切视频或切标签页，旧任务立即停止，其截图、报告、toast 和进度清理都不能覆盖新任务。Bilibili 页面状态尚为 `BV+pN` 而平台语境已解析为 `BV+CID` 时，只有当前字幕轨道能同时证明同一 BVID、page 与 CID 才允许接纳该 MediaContext，避免为了字符串完全相等而丢掉标签语境。
- AI Note 缓存恢复带 revision，而且最终渲染/保存再次要求 `artifact owner key === current artifactKey`；任何身份未知或不一致的产物都 fail-closed 显示空状态。带 `sourceUrl` 的新缓存还会反向推导来源 `artifactKey`，如果能证明来源属于另一视频则拒绝恢复。Side Panel 初始直连状态也必须先通过当前标签页 URL 与运行时 `mediaKey` 的一致性检查，不能因为旧状态仍是 `ready` 就接受它。ready→empty 的防抖只允许发生在同一媒体身份，新视频或身份未知状态必须能够立即清空旧笔记。
- 内容页在 SPA 切视频时必须先提交新的 `state.mediaKey / state.url`，再发布会话快照或 loading/ready 状态；禁止“B 视频的字幕快照 + A 视频的 mediaKey”这种短暂混合状态进入广播链。
- Service Worker 的 `BSE_GET_ACTIVE_STATE` 会重新查询真实活动 tab；`getTabState()` 只有在 `isSameMediaOrUrl()` 明确证明缓存仍属于当前媒体时才允许回退。SPA 切到新视频、或退回站点普通页面后 content script 暂时失联，不会重新返回上一媒体缓存。

### AI 提示词协议

- **自然语言优先，机器结构最小化**：学习报告、摘要、复盘题和 ASR 校对都使用普通 Markdown / 自然中文描述；只有客户端必须解析的规划结果使用 JSON。目标是减少提示词歧义和无意义的“系统级语气”，不是绕过任何安全机制。
- **MediaContext 是视频处理的统一语义事实源**：Bilibili `x/tag/archive/tags` 与 YouTube `videoDetails.keywords` 都归一为 `MediaContext.tags`，并与标题、当前分段、分类一起供 ASR、翻译、学习/复习和外部 AI 复用。作者/频道、平台和长简介仍可保存在 Context/产品状态中供 UI 使用，但 Prompt 序列化通过 `formatPromptContext()` 只输出标题、标签及少量必要主题信息。AI 操作开始前 Side Panel 通过 `BSE_GET_MEDIA_CONTEXT` 对锁定的 `tabId + mediaKey` 做一次按需刷新，消除字幕先就绪而标签仍在异步加载的竞态；
- **时间戳由任务能力决定，而不是字幕的固定格式**：阶段一章节/取样规划必须使用 `00:12  文本`；`course_notes` 只要存在有效章节骨架，阶段二也保留时间并先按章节范围分组字幕，因为时间同时承担章节边界和媒体回跳语义；真实截图时间继续用于 `frame://` 对齐。五种纯文本学习/复习任务仍只发送字幕正文，不携带逐句时间，也不要求模型额外生成 `[MM:SS]`。ASR 回填另用稳定行号 `L0001 | 文本`；报告图片继续使用 `![说明](frame://00:12)`，章节大标题使用 `## [00:12–02:35] 标题`；
- **字幕校对复用既有时间轴，不重新做声学对齐**：`generateSubtitlePolishPrompt()` 给当前 cue 分配稳定 `Lxxxx` 行号，并要求模型只返回真正修改的行；`applyPolishResult()` 直接把这些稀疏 patch 写回原 cue 的 `content`，`from/to`、分句和顺序全部复用。外部结果应用时必须同时校验 `mediaKey / selectedTrackId / cueRevision`，避免用户切视频、切轨道或字幕刷新后把旧结果写进新状态。字幕正文缓存与用户校对 overlay 使用不同持久化键：正文继续使用 `bse_sub_cache_<artifactKey>`，每轨 sparse correction 使用 `bse_sub_corrections_<artifactKey>`；普通正文刷新/淘汰不得重写或删除用户 correction。旧版内嵌在正文 record 中的 corrections 只在第一次遇到时迁移，并由 `correctionSchema` 标记避免后续重复读取大正文。Native 合同里的 `aligning` 仍属于 ASR 从音频形成时间轴的阶段，不用于已有时间轴的纯文本校对。
- **保持兼容但不继续扩散旧协议**：校对解析器继续接受旧 `[1] 文本` 和“行数与现有 cue 完全一致”的全量文本，报告渲染/删除/导出继续接受旧 `[SCREENSHOT: ...]`，因此历史缓存和旧模型返回不会突然失效；新校对任务统一使用 `Lxxxx | 文本` 的稀疏返回协议，无法证明行归属时 fail-closed，不做模糊猜测。
- **内容自适应，不用数学模板统治所有视频**：提示词不要求每个视频都输出“定理、避坑、解题动作”。章节、总结、清单、复盘题和图片密度都由内容决定。
- **Prompt 所有权单一**：学习/复习六种产物只由 `BSE.Ai.buildCourseNotePrompt()` 按 mode 构建；字幕校对无论本机 LLM 还是外部 AI 都通过 `BSE.Formatters.generateSubtitlePolishPrompt()` 生成同一任务协议，并通过 `BSE.AsrPolisher.applyPolishResult()` 回填。播放器旁面板不再复制一套 notes/summary/keypoints/questions Prompt 库，避免同一学习意图在两个模块逐渐漂移。

### AI 配置与模型状态契约

- **连接共享，模型按 Workspace 分流**：`endpoint / apiKey` 属于 AI 连接层，由学习与复习共享；`learnModel / reviewModel` 分别属于两个一级 Workspace。模型选择规则由 AI 核心的 `resolveAiModel()` / `aiScopeForMode()` 统一维护，Side Panel 只展示和提交配置，不再实现第二套路由判断。旧 `model` 字段继续保留为兼容回退及字幕精修等通用 AI 能力的默认模型，不复制两套凭证，也不让两个页面表面独立、底层却暗中覆盖同一个模型。
- **已保存模型是生成事实源**：顶部模型徽标显示“学习/复习 · 当前已保存模型”，切换一级 Workspace 时同步切换。服务目录探测不得把它自动改成“推荐模型”。任一连接或 Workspace 模型存在未保存修改时，生成按钮拒绝继续，避免用户以为新配置已经生效。
- **“服务探测”和“测试当前模型”是两件事**：`probeLlm()` 只读取 `/models` / Ollama `/api/tags`，用于协议和模型目录提示；`testLlm()` 会对输入框里的准确模型发送一次最小文本请求，并返回请求模型、服务实际返回模型、延迟和短响应摘要。测试不会自动保存配置。
- **模型目录不是最终真值**：网关可能隐藏模型列表或允许别名路由，因此“目录未发现”只显示提醒；只要实际模型调用成功，测试仍报告可用，同时把 `requestedModel → returnedModel` 的路由差异展示给用户。
- **文本测试不等于视觉能力测试**：图文讲义仍要求所选模型支持图片输入；设置面板必须明确这一边界，不用一次文本 Ping 冒充多模态能力验证。
- **网络权限保持最小化**：当前 Manifest 只授权本地 AI 网关，配置层拒绝把任意公网 Base URL 当成可直连端点。云端模型应由用户自己的本地 OpenAI-compatible 网关转发；除非以后另行设计可解释的 optional host permissions，不扩大到全网访问。
- **超时语义一致**：后台代理遵守调用方的短探测超时（下限 1 秒、上限 300 秒），不再把 3–15 秒的探测强制拖到至少 60 秒；正式生成继续使用配置中的长任务超时。

## 五、后台队列与 Native Messaging

Service Worker 是 `processPendingJobs()` 的唯一调用者。入队、重试、扩展启动和相关存储变化只负责唤醒 orchestrator；同一时刻最多有一次 drain，执行期间的新唤醒会合并为一次尾部补跑。旧 `offscreen/*` 文件仅作为历史兼容/回滚材料保留，当前运行时不创建 Offscreen Document，也不再申请 `offscreen` 权限；不得重新把它接回 Queue 执行链形成第二执行器。

队列持久化以“索引 + 每任务独立记录”为源数据，不在常规读取时扫描整个 `storage.local`。存储结构带 schema 版本，只有旧数组、旧索引或 schema 升级时才执行深度清洗迁移；完成任务只落盘规范化 cue，Markdown/TXT/SRT 作为确定性投影按需恢复并受内存预算约束，避免多份长文本重复占用持久存储。

队列存在两种明确处理意图，不能混用：`auto` 走“人工平台字幕 → 自动 CC/ASR → 平台翻译字幕 → YouTube 原生 Transcript → 本机 ASR”的 caption-first 策略；`local-asr` 表示用户明确要求离线转录，必须跳过打开标签页字幕、`stageArtifacts` 字幕缓存、平台字幕接口和 YouTube 原生字幕查询，直接对当前目标媒体执行本机 ASR。播放器与 Side Panel 的“离线转录”按钮必须发送 `processingIntent: 'local-asr'`，普通批量任务默认仍为 `auto`。YouTube 页面链路耗尽后，host 会通过固定版 `yt-dlp` 在后台重新盘点相同优先级，逐轨下载并验证 JSON3/VTT；坏轨清理后继续下一轨，因此标签页关闭或首轨损坏都不等于“无字幕”。只有规范化后至少一条 cue 且纯文本非空，任务才进入 `done`。结果同时保存语言与 `manual / auto / translated` 类型。Bilibili 的 CDN 签名地址只存在于当前执行内存；崩溃恢复会重新解析，不从存储复用过期签名。

扩展与 `com.sparksub.transcriber` 当前使用 **`sparkscribe.browser-native/2`**。Host 名保持 SparkSub 作用域，用来表示浏览器集成端点；它不等于 SparkScribe 的 App Bundle ID。

- 请求动作仍保持最小集合：`capabilities`、`ping`、`youtubeCaptions`、`transcribe`、`cancel`；每条都带唯一 `requestId`，长任务另带 `jobId`。`youtubeCaptions` 可携带受限枚举 `subtitlePreference`（`manual-first / manual-only / ai-first`）。
- **v2 capability 只描述能力，不暴露具体模型路由**：`features.localASR.languages`、`youtubeCaptions`、`remoteMedia`、`cancellation`、`chunkedResults`。浏览器不得依据 Qwen / Parakeet / Cohere / sherpa 等模型名决定语言或执行路径。
- SparkSub 默认先发协议 v2。只有 Host 明确返回 `PROTOCOL_MISMATCH` 时才允许回退一次 v1；超时、断连、坏结果或普通执行错误不得触发旧协议回退。standalone `SparkSubHost` 继续接受 v1/v2，并把 v1 的 `ytDLP + models.*` 能力立即归一成 v2 的 feature view。
- `engine` 只是可选的机器诊断标识，`engineLabel` 只是可选显示文案；二者都不是路由输入。语言支持以 `features.localASR.languages` 为事实源，因此未来 SparkScribe 增加粤语模型时不需要再改浏览器路由代码。
- 进度携带 `requestId`、`jobId`、稳定 stage、percent 与脱敏 hint，并续期队列租约及 JS 活动看门狗。
- 结果：`resultBegin` → 有序编号的 `resultChunk` → `resultEnd`。扩展验证分块数量、序号、cue 时间和非空正文后才提交。
- 原生 host 单条输出严格小于 900 KiB；输入帧上限 64 MiB；媒体文件从不经过 Native Messaging。
- 转录阶段 host 周期性发心跳。扩展使用 15 分钟静默看门狗；真正超时时先发送同一协商版本的 best-effort `cancel(jobId)`，再以稳定错误结束。
- 活动或并发请求复用同一 Native Messaging Port；最后一个请求结束 250 ms 后自动断开，避免空闲端口持续保活 Service Worker 与 Swift host。能力快照保留在内存，不因空闲断开而重新启动 host。
- 每个本机任务使用独立 UUID 临时目录并在结束或取消时清理；host 启动时只回收 SparkSub 自有临时根目录下超过 24 小时的 UUID 目录，不触碰其他命名目录。
- 正常模式 stdout 只写小端长度前缀 JSON 帧。SparkScribe bundled helper 自身保持轻量，重 ASR 通过 `SparkScribe --browser-inference-worker` 执行；worker 在模型推理期间从文件描述符层隔离第三方 stdout，再仅返回一个私有 JSON envelope。standalone `--diagnose` 仍是兼容 Host 的非分帧诊断模式。

完整消费侧契约见 `docs/native-contract-v2.md`。SparkScribe 侧使用同一 Contract ID 与字段语义；默认安装器在 bundled helper readiness 完整时将 Chrome manifest 指向 `/Applications/SparkScribe.app/Contents/Helpers/SparkSubNativeHost`，否则回退 standalone Host。

YouTube 只接受规范的 `https://www.youtube.com/watch?v=<11位ID>` 公共地址；标签页解析必须同时证明请求 videoId、当前 URL、`state.mediaKey` 与 MAIN-world 返回 `videoId` 一致。SparkScribe-owned `yt-dlp` runtime 固定版本与 SHA-256，使用参数数组而非 shell，也不读取浏览器 Cookie；平台字幕与媒体获取都被限制在独立 job workspace。Bilibili 的媒体身份以精确 `BVID + CID` 为准：当前页面发起 `local-asr` 时必须先拿到 `bili:<BVID>:cid<CID>`，Queue 每次执行再通过 view API 重新解析 BVID→page→CID；`stageArtifacts` 只有 owner mediaKey 与权威身份完全一致时才允许复用，打开标签页状态也必须精确匹配同一 BVID+CID。playurl 返回若声明了其他 BVID/CID 或音频时长明显不符，必须在调用 SparkScribe 前 fail-closed。Bilibili 远程音频仍只接受白名单 HTTPS CDN 和受限请求头。

SparkScribe 的 Media Queue、Live Captions 与 Browser Worker 共用跨进程 inference lease；资源占用冲突返回稳定 `BUSY`，不会同时加载第二套大型本地模型。standalone Host 的旧模型目录逻辑只作为兼容路径维护。

## 六、运行时性能契约

- **普通站点页与视频页使用不同加载预算**：Feed/搜索/空间等页面只静态加载 `content/feed-injector.js`；Bilibili 明确播放路径静态加载视频运行时，YouTube watch/shorts/live 与 Bilibili 非标准同文档播放路由由轻量 bootstrap 通过 `BSE_ENSURE_VIDEO_RUNTIME` 按需补注入。后台补注入前必须先 `BSE_VIDEO_RUNTIME_PING`，已存在完整运行时时不得因 SPA 导航重复解析整套脚本。视频运行时不加载 `core/queue.js`、`core/tracker.js`、`core/asr-polisher.js`，动态注入只加载当前平台 adapter；扩展更新后的 existing-tab 修复同样先补轻量 Feed，只有真实视频页才补完整运行时。
- 内容脚本以原生 `timeupdate` 事件作为播放同步主路径，并用 `requestAnimationFrame` 合并同一帧内的更新；500ms 轻量轮询只负责发现播放器元素替换并重新挂接监听器，而且只在真实视频页存在。离开视频页必须立即释放 interval、旧 `timeupdate` listener 与 pending rAF，Feed/搜索/动态页不得为了字幕播放同步保留常驻轮询。
- 只有当前句索引变化时才跨进程发布，播放更新只携带 `activeIndex` 和 `currentTime`，不重复传输完整字幕。
- 面板和侧边栏仅更换前后两个高亮节点；字幕时间轴/阅读两种视图按 `mediaKey + trackId + cueRevision` 复用已构造 DOM，只有字幕 owner 或 `cueRevision` 真正变化时才整体失效。隐藏在学习/复习/追踪/转录 Workspace 下的字幕正文不得继续后台构造。
- 页面隐藏时暂停播放与路由轮询；手动滚动后保持“恢复跟随”，只有用户点击恢复或点击字幕跳转才会重新跟随。
- 平滑滚动的整个动画期间都标记为程序滚动，避免 YouTube 广告切换或布局抖动将其误判为用户操作。
- YouTube 前贴片或中插广告复用播放器时暂停时间轴同步，保留广告前的字幕高亮，正片恢复后再继续跟随。
- Feed 注入器首次加载允许一次全量扫描；之后 MutationObserver 只扫描新增节点、最近卡片祖先和新增子树。突发变更根超过阈值时才回退一次全量扫描，禁止每个 mutation burst 对无限信息流重复执行全页 `querySelectorAll`。
- 队列持久层只保存规范 cue 与任务状态；TXT/Markdown/SRT 等确定性派生文本只能在复制/导出等明确消费时按需物化并进入有字符预算的运行时 LRU。**Queue 核心只加载在 Service Worker**：Feed 与 Side Panel 通过 `BSE_QUEUE_GET/GET_ITEM/GET_SUMMARY/GET_SETTINGS/SAVE_SETTINGS` 消费投影或单项事实，不得重新解析 `core/queue.js`。普通 Queue mutation、执行器、Side Panel 列表与 Feed 状态同步统一使用 projection/`hydrateText: false`。pending→pending 的高频进度写入只读取并写当前 item + schema，不得每秒读取历史完成任务；stage 改变到 `done/failed`、新增、删除、重试等会改变 summary/index 的结构操作才走必要结构更新。Runtime 消息与 storage change 的重复刷新必须合并为 single-flight；同 stage 进度广播只携带 `id/stage/progress/stageHint` 并 patch 单卡。`BSE_QUEUE_ENQUEUE` 自身已经唤醒唯一 executor，调用方不得再补发第二次 wake；Queue Workspace 只在进入一个仍有 pending 的恢复周期时允许额外发送一次 recovery wake。离开转录 Workspace 较久后，Side Panel 必须释放 Queue projection 工作集和卡片 DOM，只保留轻量 summary 维持一级导航角标。
- AI 视觉筛帧在单个请求窗口内只用低分辨率灰度签名比较候选秒数，并只编码该窗口最终 1 张高清图；多个窗口产生的高清候选只在本次生成内存中继续做全局去重/覆盖筛选。截图就绪只依赖媒体状态，不依赖页面 rAF/rVFC，因此标签页失焦不会额外制造绘制等待；轮询只作为媒体事件遗漏时的轻量兜底，并保持单一 pending timer，避免多个 `progress/timeupdate/canplay` 事件叠加出重复计时器。只有最终证据集进入多模态请求与 AI Note IndexedDB；fingerprint、评分和被淘汰候选不持久化。高清截图的 IndexedDB 恢复与写入都采用最多 2 张的小并发/小批量策略，禁止一次性把整组 Blob 与 Base64 同时物化。AI Note 索引保存轻量 mode 摘要，任务圆点不得为此读取六份完整 Markdown；同一视频短时间切换任务允许有界 record/渲染热缓存，但图文讲义不得缓存包含 base64 截图的 HTML/DOM。离开学习/复习较久后，只能释放已持久化产物的 Markdown/Base64/DOM 工作集，未保存修改必须继续保留；后台生成在隐藏 Workspace 完成时只提交数据和缓存，不得构造隐藏的长笔记 DOM。保存完成后的 LRU/容量 sweep 合并到后台执行，不阻塞导入、补图或删除后的用户反馈。

- 长字幕 LLM 精修由核心层按 cue/字符预算自动分块，用户仍只执行一次“精修/校对”意图，不暴露批大小、并发数或缓存参数。稀疏 `Lxxxx` 导入的预检查只解析 patch，不物化整份新 cues；只有 content owner 最终提交时复制一次完整字幕。校对 correction 必须先 durable commit，再更新当前字幕；`SessionSnapshot` 只是秒开层，长 cues 的快照序列化放到 idle 阶段合并写入。correction overlay 永远独立于正文缓存持久化，因此“改 3 句”只写小型 patch record，不重写几百 KB/MB 的 cues；完整正文即使超过单记录预算或被 LRU 淘汰，校对仍可独立保留，下一次拿到权威 base cues 后按 cue 数量、时间锚点和原文安全重放。撤销最后一个 correction 时只删除对应 correction record，不误删仍有效的正文缓存。取消操作必须同时中止当前后台 LLM HTTP 请求，而不是只停止下一批。
- 性能策略属于实现层而不是产品设置：缓存上限、Markdown 热缓存、Blob 恢复并发、字幕分块和回收节流不得暴露成普通用户选项。用户只选择学习/复习/校对/转录等目标，系统自动选择快路径与降级路径。媒体诊断与 `MediaContext` 更新使用轻量增量消息，不得为了日志/标签变化重新发送整份 cues；后台 Queue/AI 诊断直接进入 Side Panel，不得经过内容页再次包装成 media 日志形成回声。Side Panel 恢复焦点时先比较 `mediaKey/revision/cueRevision/status/selectedTrackId` 的轻量 state meta，只有版本真变化才请求 full state。学习/复习 Type 结果状态使用 dirty + single-flight 策略：视频在非 AI Workspace 切换时只标记失效，不提前读取 AI Note index，真正进入学习/复习后才刷新一次。折叠的技术日志只更新数量，用户展开时才物化完整文本。

## 七、类型与执行上下文边界

- Chrome MV3 的 Service Worker、content script、MAIN-world bridge、sidepanel 与平台脚本属于不同执行上下文。`tsconfig.json` 使用 `moduleDetection: "force"` 与 bundler module resolution 让 `checkJs` 按文件隔离分析，禁止为了消除虚假的全局重名错误而给运行时代码增加无意义 namespace 包装。
- DOM 类型必须在真实 seam 收窄：侧边栏使用集中元素注册表，滚动面板通过 `requireElement(selector, ctor)` 同时做模板运行时校验和具体元素类型收窄；委托事件先把 `EventTarget` 收窄成 `Element` / `HTMLInputElement`。禁止用全局 `any`、`@ts-ignore` 或给所有 `HTMLElement` 添加私有字段来压平类型债。
- Native Host 转录结果使用显式 `{ cues, engine }` 对象；不得继续给数组动态挂 `engine` 属性。调用端与类型声明必须共享同一结果模型。
- Bilibili 与 YouTube 批量目录统一归一到 `BatchMediaTree`；平台适配器负责消化原始拓扑差异，侧栏/批量 UI 不维持两套结构分支。
- `getMediaKey(platform, url)` 的显式 URL 是契约的一部分。后台判断标签页媒体时不得静默回退到当前扩展上下文的 `location` / DOM。
- Bilibili 与 YouTube 的平台 Module 只负责把原始目录和字幕抓取结果归一成 `BatchMediaTree` / `BatchItemResult`。条目范围选择、稳定排序、Manifest、复制长文本、合并 Markdown/TXT 与 ZIP 的组织规则统一由 `core/batch-export.js` 承担；Side Panel 和播放器旁面板只是两个 delivery Adapter，不得再让平台 Module 直接触发文件下载。
- YouTube 批量字幕请求必须把用户的 `manual-first / manual-only / ai-first` 偏好和 `AbortSignal` 透传到 Native Host。取消批量任务必须取消正在执行的 `youtubeCaptions` 作业，而不只是停止领取下一条。

## 八、必须维持的不变量

1. 只有 `cues.length > 0` 时才能进入 `ready`。
2. 每次媒体切换都必须提高任务代数并取消旧请求。
3. 旧媒体或旧轨道的结果不得写入当前状态。
4. 诊断不得包含完整字幕网址、签名或 PO Token。
5. 页面主世界通信桥不得替换 `fetch` 或 `XMLHttpRequest`。
6. 用户手动滚动后，自动跟随不得立即抢回滚动位置。
7. 已有字幕刷新时不得清空正文；失败后必须回到可用状态并保留旧结果。
8. 后台不得接受非哔哩哔视频页发起的代理请求，也不得访问白名单外域名。
9. 订阅追踪卡片默认只渲染最近 3 条记录，历史必须通过明确的“查看其余记录”入口在侧边栏内展开；UI 折叠不得等同于删除历史。普通订阅最多保留 20 条历史，合集可保留最多 100 条以支撑稳定增量差分与批量工作流。
10. 无人值守后台抓取必须捕获并封装异常，单视频字幕失败不得中断全量巡检流程。
11. 只有 Service Worker 可以执行队列；侧边栏、内容脚本与 Offscreen Document 只能入队或唤醒。
12. 平台字幕成功时不得下载音频或调用本机 ASR。
13. 本机语言支持必须读取协商后的 `features.localASR.languages`；未声明支持的语言在平台字幕耗尽后返回 `ASR_LANGUAGE_UNSUPPORTED`。不得在浏览器侧把粤语或任一语言永久绑定为“支持/不支持”，也不得据模型名猜路由。
14. Native Messaging 不得携带媒体字节、Cookie、Authorization 或持久化签名 URL。
15. 本机任务的取消、超时、EOF 和失败路径都必须清理任务专属临时目录。
16. 移除、清空或重试活动任务必须经 Service Worker 取消旧执行；旧租约不得覆盖新状态或复活已删除任务。
17. AI 视觉请求中的 `0` 秒是合法时间戳；窗口、目标秒数与截图时间必须用有限数值/空值判断，不得用 truthy/falsy 判断误丢视频开头的证据点。
18. 规划阶段可以宽覆盖视觉候选，但自动候选只有经过全局筛选后才能进入多模态请求和长期图片缓存；用户手动截图不得被自动去重逻辑静默删除。
19. 新提示词不得重新混用字幕时间、逐句回填 ID 与图片引用语法；`00:12  文本`、`L0001 | 文本`、`frame://00:12` 分别承担独立职责。
20. Tracker 的逐条 `isRead` 是已读状态真值，`unreadCount` 只能由当前保留条目的状态派生；单条已读写入必须幂等，禁止用 `unreadCount--` 或“前 N 条”推断真实未读集合。
21. Tracker 巡检可以重复拉取轻量目录用于差分，但只有稳定 ID 未见且位于已读水位线之后的条目才能成为新未读；自动字幕提取等昂贵工作只能发生在这组真正新增未读条目上。
22. 批量目录、预览和导出不得承担 Tracker 的更新发现职责，也不得整体替换 `TrackedSubscription.items`。目录元数据回填必须按稳定条目 ID 合并并保留 `isRead`、`subtitle` 等持久状态。
23. 多 P 条目必须使用稳定页级 ID（包括 p1）；用于平台根查询的 BVID 字段必须保持纯 BV，不得混入 `:pN` 条目后缀。旧 ID 迁移必须保留既有已读与字幕状态。
24. Tracker 主存储 `bse_subscriptions` 只保存标题、稳定 ID、发布时间、已读状态与字幕状态等轻量账本元数据；字幕正文必须按媒体键存入 `UnifiedSubtitleCache`。复制/预览按需取正文，禁止因一次已读写回而重写整批字幕文本。
25. Tracker 的 runtime 通知、storage change、窗口聚焦等重复刷新必须合并为 single-flight/debounce；非 Tracker 页只允许刷新轻量未读摘要，不得无条件重渲染完整追踪列表。
26. 全量订阅巡检必须使用受控小并发而不是串行人工延迟或无界并发；同一轮手动/Alarm 巡检继续共享唯一 in-flight 任务。
27. 播放器截图可靠性的事实源只能是 `core/media.js`：Side Panel 不得复制 `readyState/buffered` 判定、瞬态错误集合或重试次数。外部规划与内置规划必须共享同一个阶段一媒体执行 seam。
28. 报告中的 `frame://`/插槽时间是语义引用位置，`CapturedFrame.timestamp` 是实际截到的播放器时间；两者允许轻微偏移。保存和回跳必须保留真实截图时间，不得为了匹配引用而覆盖它。

## 九、验证边界

Node、Shell 和静态协议测试可在当前工作区执行。Swift/CoreML 编译、XCTest、真实模型 schema/load、真实媒体解码和 Chrome Native Messaging 安装链路必须在 macOS 14+ Apple Silicon 上验证；没有这些证据时不得写成“已实机通过”。

2026-09-10 本机执行接管的可复现验证结果：`node tests/run-tests.mjs` 与 `tests/install-host-tests.sh` 全部通过；standalone `native/SparkSubHost` 的 `swift test` 执行 58 项、0 failure、2 skipped，跳过项仅因 `/tmp/test_speech.wav` 旧模型推理夹具不存在；SparkScribe 的 canonical `build.sh --check` 执行 365 tests / 58 suites 全部通过。打包后的 SparkScribe worker 已用真实短语音完成 Qwen3-ASR timed-cue smoke，bundled helper 的 v2 capabilities/framing、签名、yt-dlp readiness 与真实 Chrome manifest 切换也已验证。当前仓库仍未固定 TypeScript compiler，因此 `tsc -p tsconfig.json` 不能列为可复现 Gate；另外 direct helper/worker smoke 不替代最后的 Chrome 扩展 UI → Native Messaging → SparkScribe → cues 人工 E2E。

## 十、后续扩展方式

- 新增平台时，实现与 `discoverTracks` / `loadTrack` 相同的适配器边界。
- 新增导出格式时，只修改 `core/formatters.js` 与侧边栏选项。
- 迁移批量导出时，使用独立任务模块，不将批量队列写入当前视频状态。
- 新增订阅平台时，在 `core/tracker.js` 中扩充增量比对与 `fetchItemSubtitle` 适配器。
